<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.eatgreen">

    <!-- PERMISSIONS (doivent être en dehors de application) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <permission
        android:name="com.example.eatgreen.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION"
        android:protectionLevel="signature" />

    <uses-permission android:name="com.example.eatgreen.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- APPLICATION (tout le reste doit être DANS application) -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EatGreen"
        android:usesCleartextTraffic="true"
        android:requestLegacyExternalStorage="true">

        <!-- ACTIVITIES -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ResetPasswordActivity" android:exported="false" />
        <activity android:name=".ForgotPasswordActivity" android:exported="false" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".CreateRestaurantActivity" />
        <activity android:name=".RestaurantActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".EtudiantActivity" />
        <activity android:name=".EditerProfilEtudiantActivity" />
        <activity android:name=".ModifierRestoActivity" />
        <activity android:name=".CbActivity" />
        <activity android:name=".PaypalActivity" />
        <activity android:name=".GestionUtilisateursActivity" />
        <activity android:name=".ModifierUserActivity" />
        <activity android:name=".Potager_coursJardinageActivity" />
        <activity android:name=".BoutonActivity" />
        <activity android:name=".FormCalendrierActivity" />
        <activity android:name=".StatistiquesActivity" />
        <activity android:name=".EvenementsActivity" />
        <activity android:name=".PublierPanierActivity" />
        <activity android:name=".DateHeureActivity" />
        <activity android:name=".AffichagePanierActivity" />
        <activity android:name=".PanierActivity" />
        <activity android:name=".CommanderActivity" />
        <activity android:name=".PaymentActivity" />
        <activity android:name=".Publier_coursJardinageActivity" />
        <activity android:name=".Inscription_coursJardinageActivity" />
        <activity android:name=".ListeInscritCoursJardinageActivity" />
        <activity android:name=".MapComposteActivity" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.eatgreen.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- InitializationProvider (automatique) -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="com.example.eatgreen.androidx-startup"
            android:exported="false"
            tools:ignore="MissingClass">
            <meta-data
                android:name="androidx.emoji2.text.EmojiCompatInitializer"
                android:value="androidx.startup" />
            <meta-data
                android:name="androidx.lifecycle.ProcessLifecycleInitializer"
                android:value="androidx.startup" />
            <meta-data
                android:name="androidx.profileinstaller.ProfileInstallerInitializer"
                android:value="androidx.startup" />
        </provider>

        <!-- Profile Installer Receiver -->
        <receiver
            android:name="androidx.profileinstaller.ProfileInstallReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.DUMP"
            tools:ignore="MissingClass">
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.INSTALL_PROFILE" />
                <action android:name="androidx.profileinstaller.action.SKIP_FILE" />
                <action android:name="androidx.profileinstaller.action.SAVE_PROFILE" />
                <action android:name="androidx.profileinstaller.action.BENCHMARK_OPERATION" />
            </intent-filter>
        </receiver>

    </application>

</manifest>